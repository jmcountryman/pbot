/*
 * An example config file. Rename this to "config.js" and fill in the empty values before running
 * this bot.
 */
module.exports = {
    /**
     * Required. You can find this token in your application's details page, in the "Bot" section.
     */
    token: '',

    /**
     * Required. You can find your app's client ID in its details page, in the "OAuth2" section.
     */
    clientId: '',

    /**
     * Optional. If this value is set, slash commands will be deployed only to that guild;
     * otherwise, slash commands will be deployed to all guilds this bot has joined.
     */
    guildId: ''
};
